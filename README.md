# 宏观经济指标分析平台

个人宏观经济指标分析网站，基于 Python FastAPI + Vue3 构建。

## 功能特性

- 📊 实时数据接入（akshare）
- 📈 多维度经济指标展示（GDP、CPI、PPI、PMI、M2等）
- 🔔 预警系统（自定义阈值监控）
- 💡 投资决策辅助（趋势分析、情景模拟）
- 🎨 现代化仪表盘界面

## 技术栈

### 后端
- Python 3.10+
- FastAPI
- SQLAlchemy + SQLite
- akshare（数据源）
- JWT 认证

### 前端
- Vue 3 + TypeScript
- Vite
- Element Plus
- ECharts / vue-echarts
- Pinia
- Vue Router

## 快速开始

### 方式一：本地直接运行

1. **安装后端依赖**
```bash
cd backend
pip install -r requirements.txt
```

2. **启动后端服务**
```bash
cd backend/app
uvicorn main:app --reload
```
后端服务将在 http://localhost:8000 运行

3. **安装前端依赖**
```bash
cd frontend
npm install
```

4. **启动前端服务**
```bash
npm run dev
```
前端服务将在 http://localhost:5173 运行

5. **访问系统**
打开浏览器访问 http://localhost:5173

### 方式二：Docker Compose

```bash
docker-compose up -d
```

- 前端：http://localhost:5173
- 后端：http://localhost:8000

## 默认指标

| 指标代码 | 名称 | 类别 |
|---------|------|------|
| gdp | 国内生产总值(GDP) | 经济增长 |
| cpi | 居民消费价格指数(CPI) | 物价水平 |
| ppi | 工业生产者出厂价格指数(PPI) | 物价水平 |
| pmi | 采购经理指数(PMI) | 经济景气 |
| m2 | 广义货币(M2) | 货币金融 |
| interest_rate | 利率 | 货币金融 |
| trade | 进出口贸易 | 对外贸易 |
| consumer_sentiment | 消费者信心指数 | 经济景气 |
| stock_market | 上证指数 | 金融市场 |

## API 文档

启动后端后访问：http://localhost:8000/docs

## 项目结构

```
itrade/
├── backend/
│   ├── app/
│   │   ├── api/          # API路由
│   │   ├── core/          # 核心配置
│   │   ├── models/        # 数据模型
│   │   ├── schemas/       # Pydantic模型
│   │   ├── services/      # 业务服务
│   │   └── main.py        # 应用入口
│   ├── data/              # 数据缓存目录
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── api/           # API调用
│   │   ├── components/    # 组件
│   │   ├── router/        # 路由
│   │   ├── stores/        # 状态管理
│   │   ├── types/         # TypeScript类型
│   │   └── views/         # 页面视图
│   ├── package.json
│   └── Dockerfile
├── docker-compose.yml
└── README.md
```

## 扩展指标

在 `backend/app/services/data_fetcher.py` 中添加新的数据获取方法即可扩展指标。

## 部署到阿里云

1. 安装 Docker 和 Docker Compose
2. 推送代码到阿里云服务器
3. 使用 Docker Compose 启动服务
4. 配置域名和 HTTPS

## License

MIT
